<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="ThemeComment" parent="Theme.AppCompat.Light.Dialog">
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <style name="AppTheme.ButtonCancel" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#ff0000</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="AppTheme.ButtonAccepted" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#008000</item>
        <item name="android:textColor">#ffffff</item>
    </style>

</resources>
